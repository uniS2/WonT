export interface TotalScheduleTitleProps {
  localName: string;
  startDay: string;
  endDay: string;
}

export interface TotalScheduleImageList {
  imageURL: string;
  localName: string;
  className: string;
}
